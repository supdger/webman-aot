{
  "schema": "webman-aot-local-test-run-v1",
  "date": "2026-09-25",
  "scope": "Current uncommitted development worktree; this result is not a clean-checkout or release-tag verification",
  "host": "macos-arm64",
  "php": "8.4.18 CLI",
  "branch": "feat/full-static-sdk-cross-host",
  "baseCommit": "c8e8b91",
  "command": "php tests/run.php",
  "exitCode": 0,
  "passed": 44,
  "failed": 0,
  "elfGuardFollowUp": {
    "checks": "Rejects a relocatable ELF even when it has PT_LOAD; accepts executable and static-PIE ELF types only when other static checks pass",
    "realSaiAdminElf": "pass: current fully static SaiAdmin server accepted after the guard change",
    "limits": "ELF structure checks do not prove service startup or business behavior"
  },
  "acceptanceScriptJsonSelfTest": {
    "command": "sh -n tests/accept-saiadmin-linux.sh && sh tests/accept-saiadmin-linux.sh --self-test",
    "macosExitCode": 0,
    "result": "PASS acceptance JSON parser",
    "guard": "SaiAdmin permission denial requires business code 400 and type failed; business 500 is rejected",
    "limit": "Current strict permission guard was self-tested on macOS; the earlier parser revision passed Python 3.6 on a CentOS 7 virtual machine"
  },
  "limits": [
    "The worktree contains tracked and untracked implementation changes, so the base commit alone does not reproduce this exact test state.",
    "Unit and contract tests do not replace target-host runtime or business acceptance."
  ]
}
